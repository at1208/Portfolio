import React from 'react';

const Button = (props) => {
  return <div className=''>
      <button className='btn btn-sm btn-outline-primary'>{props.name}</button>
         </div>
}
export default Button;
